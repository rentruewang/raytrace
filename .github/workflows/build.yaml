name: Build
on: [push]
jobs:
  format-all:
    runs-on: ubuntu-latest
    steps:
      - name: 🔔 Check out
        uses: actions/checkout@v2

      - name: 🏗️ go runtime
        uses: actions/setup-go@v2
        with:
          go-version: ^1.13.1
      - name: 🏃 go build
        run: go build
